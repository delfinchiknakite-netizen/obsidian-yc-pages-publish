name: Release Obsidian plugin

on:
  push:
    tags:
      - '*'

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Собираем самодостаточный main.js из src (вшивает qrcode, markdown-it, модули, шаблоны)
      - name: Build bundle
        run: node src/build.js main.js

      - name: Verify manifest version matches tag
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          ver="$(node -p "require('./manifest.json').version")"
          if [ "$tag" != "$ver" ]; then
            echo "Tag ($tag) != manifest version ($ver)"; exit 1
          fi

      # Провенанс сборки: доказывает, что main.js собран из этого репозитория
      - name: Attest build provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: main.js

      # Obsidian скачивает только main.js и manifest.json (+styles.css при наличии)
      - name: Create GitHub release with plugin assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          gh release create "$tag" main.js manifest.json \
            --title "$tag" --notes "Automated release $tag" \
          || gh release upload "$tag" main.js manifest.json --clobber
